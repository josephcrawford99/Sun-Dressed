# Weather-Based Wardrobe Assistant (MVP Focus)

## Project Architecture
- Expo/React Native with TypeScript
- State Management: Context API (Redux only after MVP)
- Navigation: React Navigation
- API Integration: OpenWeatherMap
- Local Storage: AsyncStorage (Firebase after MVP)
- Use STRUCTURE.md for the file structure and adjust it to match industry standards for mobile apps
- Add file names to STRUCTURE.md when creating new files

## Development Workflow
- Follow specifications.md strictly for feature tracking
- Complete all MVP features before advancing to "Further features"
- Mark tasks as completed with [x] in specifications.md
- Add tests before marking features complete
- Ask the user relevant queries to reduce token usage. The questions will be fed to an external web searching and reasoning tool called perplexity pro.

## Development Environment
- Default to using PowerShell, C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe
- When missing dependencies or tools, install them and if unable, pause and ask the user to install them manually
- Navigate the file system by consulting STRUCTURE.md and standard command line navigation (using cd, ls, ../.., etc.)
- I have installed node, npm, and yarn. If these fail to work, pause and ask the user to install them manually

## Git Workflow
- Main branch: stable releases only
- Develop branch: integration branch
- Feature branches: named feature/[feature-name]
- Test branches: named test/[feature-name]
- Commit message format: "[Feature/Fix/Refactor]: Brief description"

## MVP First Approach
- No implementation of "Further features" until ALL MVP items are complete
- Validate each MVP feature with manual testing before marking complete
- Document all completed features in README.md
